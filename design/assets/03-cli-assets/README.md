# GodeX CLI Design Assets

Canonical startup command:

```bash
$ godex
GodeX is running. Listening on http://localhost:5678
```

Use `godex-cli-badge.svg` for website / README previews and `godex-cli-badge-760x240.png` for raster mockups.
