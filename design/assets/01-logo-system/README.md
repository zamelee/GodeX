# GodeX Logo System v2 Corrected

Canonical brand values:

```json
{
  "name": "GodeX",
  "slogan": "Make every model a Codex engine.",
  "subtitle": "OpenAI-compatible Responses API gateway.",
  "logoSubtitle": "OPENAI-COMPATIBLE RESPONSES API GATEWAY"
}
```

CLI preview assets in this package use:

```bash
$ godex
GodeX is running. Listening on http://localhost:5678
```

Recommended files:

- `svg/godex-logo-horizontal-gradient.svg` — website / README on light backgrounds
- `svg/godex-logo-horizontal-on-dark.svg` — dark Hero / terminal surfaces
- `svg/godex-icon-mark-gradient.svg` — icon-only mark
- `png/godex-cli-badge-760x240.png` — corrected CLI badge preview
- `app-icons/godex-app-icon-1024.png` — application icon
- `favicon/favicon.ico` — browser favicon
